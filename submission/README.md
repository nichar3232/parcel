# Oddlot submission package

Current entry: [ENTRY.md](ENTRY.md). Current video: [oddlot-demo.mp4](oddlot-demo.mp4). Captions: [oddlot-demo.vtt](oddlot-demo.vtt). Validation: [VALIDATION.md](VALIDATION.md).

The video records the current Oddlot UI against its backend and new local-validator program. The older Bellwether/Strata entry and video are archived under `legacy/`. Other original chain evidence describes that older program and remains historical evidence only.

Run `python3 scripts/package-submission.py` from the repository root to generate the source archive and checksums. Signing keys, databases and local runtime files are excluded. Public repository access, hosting and hackathon submission require publication authorization.
